import React from 'react'
import ReactLoading from 'react-loading';

const Loading = () => {
    return (
        <ReactLoading type={'spinningBubbles'} color={'blue'} height={'40px'} width={'40px'} />
    )
}

export default Loading